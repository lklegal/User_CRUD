CREATE TABLE Users(
	userID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    userName VARCHAR(50) NOT NULL,
    userEmail VARCHAR(150) NOT NULL UNIQUE,
    userPassword VARCHAR(64) NOT NULL,
    userSalt VARCHAR(20) NOT NULL
);